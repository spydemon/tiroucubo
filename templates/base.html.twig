<!DOCTYPE html>
<html lang="{{ page.lang }}">
    <head>
        <meta charset="UTF-8">
        <meta name="author" content="{{ page.author }}" />
        <title>
            {% if page.title %}
                {{ page.title }} —
            {% endif %}
            {{ website.title }}
        </title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            {% block header %}
                {# TODO: rename to header #}
                {{ render(controller( 'App\\Controller\\Fragment\\Navigation::display')) }}
            {% endblock %}
        </header>
        <div class="notification">
            {% block notification %}
                {{ render(controller( 'App\\Controller\\Fragment\\Notification::display')) }}
            {% endblock %}
        </div>
        <div class="left-column">
            {% block left_column %}
                <ul>
                    <li>Installation</li>
                    <ul>
                        <li>Docker configuration</li>
                        <li>Composer use</li>
                        <li>Magento configuration</li>
                    </ul>
                    <li>Use of the CMS</li>
                    <ul>
                        <li>All about customers</li>
                        <li>Product configuration</li>
                    </ul>
                    <li>Module creation</li>
                    <li>Front modifications</li>
                    <li>etc.</li>
                </ul>
            {% endblock %}
        </div>
        <div class="main-content">
            {% block body %}{% endblock %}
        </div>
        <div class="right-column">
            {% block right_column %}
                <div class="newsletter">
                    <p>Feel free to subscribe to my (currently) useless mailing list!</p>
                    <input type="email" placeholder="Your email address" /><br />
                    <button type="submit">Subscribe!</button>
                </div>
            {% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
        <footer>
            {% block footer %}
                {{ render(controller( 'App\\Controller\\Fragment\\Footer::display')) }}
            {% endblock %}
        </footer>
    </body>
</html>
